import { Layout } from '@/components/core/Layout';
import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <Layout>
      <h2>Hello World</h2>
    </Layout>
  );
};

export default Home;
